---
dependency:
  name: galaxy
driver:
  name: docker
platforms:
  - name: ansible-role-ubuntu-20.04
    image: python:3.9-buster
    privileged: true
provisioner:
  name: ansible
#lint: |
#  set -e
#  yamllint .
#  ansible-lint .
verifier:
  name: ansible
